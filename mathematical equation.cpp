#include <stdio.h>
#include<math.h>
using namespace std;
int main()
{ 
//mathematical equation
int a=24,b=32,c=56,x=46,y=98,d=34;
float ans1;
ans1=((7 * 7 * b) * ((x * y) + a)) / (((c - 0.8) + (2 * b * d)) * ((x + a) * (1.0 / y)));
printf("%f",ans1);
return 0;
}
